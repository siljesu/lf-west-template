target C {
    platform: {
        name: linux,
        board: mimxrt1170_evk_cm7,
        port: /dev/enp4s0f2,
     },
    threading: true,
    workers: 2,
}

main reactor {
    timer t(1 sec,1 sec);
    reaction(startup) {=

        printf("Hello ");
        lf_sleep(MSEC(1000));
        printf("World\n");
    =}
    reaction(t) {=
        printf("Time\n");
    =}
}

