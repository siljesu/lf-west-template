// This is a smoke test of a minimal reactor.
target C{
    platform: "Zephyr",
    threading: true,
    //workers: 1,
}

main reactor MinimalThreaded {
    timer t

    reaction(t) {= printf("Hello World.\n"); =}
}
